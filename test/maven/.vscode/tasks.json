{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "java (export)",
            "label": "java (export): maven",
            "mainClass": "com.mycompany.app.App",
            "targetPath": "${workspaceFolder}/${workspaceFolderBasename}.jar",
            "elements": [
                "${compileOutput}",
                "${dependencies}"
            ],
            "problemMatcher": []
        }
    ]
}
